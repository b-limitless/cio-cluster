apiVersion: apps/v1
kind: Deployment
metadata:
  name: pasal-orders-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pasal-orders-mongo
  template:
    metadata:
      labels:
        app: pasal-orders-mongo
    spec:
      containers:
        - name: pasal-orders
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: pasal-orders-mongo-srv
spec:
  selector:
    app: pasal-orders-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
